// Hello, World in an F# script.

/// Prints "Hello, World {name}" to the Console.
let printHello(name:string) : unit =
    // replace the () below with a call to System.Console.WriteLine()
    ()

// You can then call the function as below.
// Make sure you first remember to send the above function to FSI by highlighting it
// and hitting ALT + ENTER!
printHello "isaac"
